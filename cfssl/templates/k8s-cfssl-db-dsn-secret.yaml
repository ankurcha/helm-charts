apiVersion: v1
kind: Secret
metadata:
  name: cfssl-db-dsn
  labels:
    app: {{ template "fullname" . }}
    chart: "{{ .Chart.Name }}-{{ .Chart.Version }}"
    release: "{{ .Release.Name }}"
    heritage: "{{ .Release.Service }}"
data:
  db-config: "{{ include "database_dsn" . | b64enc }}"
